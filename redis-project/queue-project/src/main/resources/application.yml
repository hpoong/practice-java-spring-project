
server:
  port: 2343

logging:
  config: classpath:logback/logback-srping.xml
  level:
    root: info

spring:
  jmx:
    enabled: false
  data:
    redis:
      host: ${Redis_Host}
      port: ${Redis_Port}
      timeout: 60000
      connect-timeout: 60000